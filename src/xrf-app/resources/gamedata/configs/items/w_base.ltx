; Reached only through the wildcard include above, so the tree must show it as included rather than
; as an entry point of its own.

[wpn_base]
$scheme = $weapon
cost = 100
kind = pistol

[wpn_child]:wpn_base
cost = 4000
kind = rifle
