; An entry point declaring a parent nothing in its own resolution holds. The engine inherits nothing
; from it silently, which is the difference the header colouring exists to show.

[wpn_orphan]:no_such_base
cost = 1
