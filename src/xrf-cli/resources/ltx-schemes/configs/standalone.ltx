; Nothing includes this file, so it is an entry point of its own and its findings name it directly.
; [$ammo] is not strict, so an absent field is not a finding and only the two written here are judged.

[ammo_9x18]
$scheme = $ammo
cost = free
box_size = 300
