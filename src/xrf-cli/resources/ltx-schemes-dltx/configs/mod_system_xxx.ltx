; The whole point of this tree: a patch can break a section that is correct in the file declaring it.
; `cost` stops being a number and `rpm` stops existing, and the strict scheme has to say so.

![wpn_ak74]
cost = expensive
!rpm
