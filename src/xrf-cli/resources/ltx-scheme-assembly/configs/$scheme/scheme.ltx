; The scheme root: nothing includes it, and it is what brings every declaration beside it into one
; resolution. The declarations it names inherit across each other, so none of them resolves alone.

#include "base.scheme.ltx"
#include "items.scheme.ltx"
