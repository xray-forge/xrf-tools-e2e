; The declaration that inherits across files: [$spawn_object] is written in base.scheme.ltx beside
; this one, so reading this file against itself finds no such section - the shape `xrf-engine` ships
; and the one that decides which resolution judges a scheme declaration.

[$item]:$spawn_object
cost = u32
